// #include "config.h"
// #include "asteroid.h"
// #include <raylib.h>

// // GetRandomValue

// void Asteroid_Init(Asteroid *p) {
//   p->position = (Vector2){(int)SCREEN_WIDTH / 2, (int)SCREEN_HEIGHT / 2};
//   p->radius = (int)SCREEN_WIDTH / 50;
//   p->speed = (float)PLAYER_SPEED;
// };

// void Asteroid_Update(Asteroid *p, float dt);

// void Asteroid_Draw(const Asteroid *p) {
//   DrawCircleV(p->position, p->radius, RED);
// };
